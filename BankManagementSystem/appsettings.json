{
  "CloudConfiguration": {
    "AWS": {
      "Region": "us-east-1",
      "Services": {
        "RDS": {
          "ProxyEndpoint": "${DATABASE_CONNECTION_STRING}",
          "Description": "RDS Proxy endpoint for connection pooling and IAM authentication"
        },
        "DirectoryService": {
          "LdapServer": "${LDAP_SERVER}",
          "LdapPort": "${LDAP_PORT}",
          "BaseDN": "${LDAP_BASE_DN}",
          "Description": "AWS Managed Microsoft AD for LDAP authentication"
        },
        "SQS": {
          "TransferAuditQueue": "${TRANSFER_AUDIT_QUEUE}",
          "CustomerInfoQueue": "${CUSTOMER_INFO_QUEUE}",
          "Description": "Amazon SQS queues replacing MSMQ for message queuing"
        },
        "S3": {
          "CustomerLogsBucket": "${CUSTOMER_LOGS_BUCKET}",
          "DepositCacheBucket": "${DEPOSIT_CACHE_BUCKET}",
          "Description": "Amazon S3 buckets replacing local file system storage"
        },
        "SystemsManager": {
          "ParameterStorePrefix": "/BankApp",
          "Description": "AWS Systems Manager Parameter Store replacing Windows Registry"
        },
        "EFS": {
          "AuditLogPath": "${AUDIT_LOG_PATH}",
          "Description": "Amazon EFS mount for shared audit logs"
        }
      }
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "System": "Warning"
    },
    "Console": {
      "IncludeScopes": true,
      "TimestampFormat": "yyyy-MM-ddTHH:mm:ss.fffZ"
    }
  },
  "ApplicationSettings": {
    "UseUtcTimestamps": true,
    "TimeZone": "UTC",
    "Description": "All timestamps use UTC (DateTimeOffset.UtcNow) for cloud consistency"
  }
}
